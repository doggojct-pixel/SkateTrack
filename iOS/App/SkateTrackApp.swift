// [協作區] SkateTrackApp.swift
// 用途：定義 SkateTrack iOS App 的最小入口點；Task-001 僅建立可建置空殼。
// 委派至：後續 Task 的 iOS/Features 與 iOS/Hooks 模組。

import SwiftUI

@main
struct SkateTrackApp: App {
    var body: some Scene {
        WindowGroup {
            EmptyView()
        }
    }
}
