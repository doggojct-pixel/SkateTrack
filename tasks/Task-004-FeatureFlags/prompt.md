# Task-004 Agent Prompt

Implement the SkateTrack feature flag system exactly as defined by Build Plan v1.0 Task-004. Use Build Plan as the source of task scope and DevProcess as the coding standard.

Create a shared gated feature enum, an iOS autonomous feature flag engine, and a SwiftUI hook boundary for views. Keep StoreKit entitlement reading stubbed until Task-016. Provide a DEBUG-only simulator toggle so premium access can be tested without purchase infrastructure.

Do not implement paywall screens, real purchases, persistence, GPS, or dashboard UI in this task.
